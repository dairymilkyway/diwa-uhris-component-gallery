/**
 * DIWA Design Tokens — barrel export
 *
 * Import individual token modules for component customization.
 * These tokens are a reference foundation — not yet consumed by
 * components. Adoption happens incrementally in future visual passes.
 *
 * Usage:
 *   import { focus, border, surface } from '@/ui-library/tokens';
 *   import { radius }                 from '@/ui-library/tokens';
 *   import { shadow }                 from '@/ui-library/tokens';
 *   import { typography }             from '@/ui-library/tokens';
 *   import { dimension }              from '@/ui-library/tokens';
 */

export * as colors    from './colors';
export * as radius    from './radius';
export * as shadow    from './shadow';
export * as typography from './typography';
export * as dimension from './dimension';
